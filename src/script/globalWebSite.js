const globalWebSite = {
    header: window.document.querySelector("header#header"),
    main: window.document.querySelector("main#main"),

    // throwErrorOnScreen(error) {
    //     if(error) {
    //         theme.applyWebsiteStyle("error");
    //     }
    // }
}